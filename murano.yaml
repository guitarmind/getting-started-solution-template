---
formatversion: 1.0.0

info:
  name: ExoInsight
  description: |
    IoT connector for Scalable Data Transformation, Machine Learning and Deep Learning on IoT data.
  authors:
    - 'exosite'
  version: 0.0.1

options:
  safeNamespace: vendor

modules:
  location: modules
  include: '**/*.lua'
  exclude: ['*_test.lua', '*_spec.lua']

services:
  location: services
  include: '**/*.lua'
  exclude: ['*_test.lua', '*_spec.lua']

init_script: init.lua
